using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sample68 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Charactor test = new Charactor();
        test.SetUp("name",100,20,15);
        
    }

    // Update is called once per frame
    void Update () {
		
	}
}
